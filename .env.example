# .env is only for sensitive e2e test values.
# Runtime configuration should use CLI flags, real environment variables,
# or config.toml — not this file.

# E2E test secret: path or content of a Solana keypair
# Accepts a file path, an inline JSON array of 64 bytes, or a 64-byte base58 string
PRIVKEY=/home/you/.config/solana/id.json

# Optional to export (do not keep in .env):
# These are safe, non-sensitive values you can export in your shell
# when running the e2e test. Keep them commented here to avoid
# accidentally treating .env as runtime config.
#
# export SOLANA_RPC_URL=http://127.0.0.1:8899
# export SUBSCRIBE_URL=http://127.0.0.1:12345
# export AMOUNT_SOL=0.01
# export SKIP_PREFLIGHT=false
