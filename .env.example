# Copy this file to `.env` and adjust values as needed.

# Network ports
UDP_PORT=18999
RPC_PORT=28899

# Optional structured trace log path inside the container
# Host path is mounted via docker-compose (./logs -> /app/logs)
TRACE_LOG_PATH=/app/logs/trace.log

# Optional: set Rust log level (info, debug, trace, etc.)
RUST_LOG=info

# E2E test configuration (used by tests/e2e_send_self.rs)
# Path to a Solana keypair JSON file, a base58-encoded 64-byte keypair, or an inline JSON array
PRIVKEY=/home/you/.config/solana/id.json
# Solana cluster RPC
SOLANA_RPC_URL=http://127.0.0.1:8899
# Shreds subscriber RPC
SUBSCRIBE_URL=http://127.0.0.1:28899
# Amount in SOL to self-transfer for the test
AMOUNT_SOL=0.01
